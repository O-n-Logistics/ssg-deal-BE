server:
  port: 8010

spring:
  application:
    name: promotion_service
  config:
    import: "configserver:"
  data:
    redis:
      host: host
      port: 1234
      username: username
      password: password
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config_service
  profiles:
    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/